<section class="hero" id="hero">
  <div class="hero-overlay"></div>
  <div class="hero-content container">
    <p class="hero-label">Адвокатско съдружие</p>
    <h1 class="hero-title">Георгиев и Зънгарова</h1>
    <div class="gold-accent"></div>
    <p class="hero-subtitle">
      Процесуално представителство, защита и правни консултации
    </p>
    <div class="hero-actions">
      <a href="#contact" class="btn btn-primary">Свържете се с нас</a>
      <a href="#practice" class="btn btn-outline">Нашите услуги</a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--color-navy-dark) 0%, var(--color-navy) 50%, var(--color-navy-light) 100%);
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background:
      radial-gradient(circle at 20% 50%, rgba(201, 168, 76, 0.08) 0%, transparent 50%),
      radial-gradient(circle at 80% 20%, rgba(201, 168, 76, 0.05) 0%, transparent 50%);
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 40px,
      rgba(201, 168, 76, 0.02) 40px,
      rgba(201, 168, 76, 0.02) 41px
    );
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: 2rem 1.5rem;
    max-width: 800px;
  }

  .hero-label {
    font-family: var(--font-body);
    font-size: clamp(0.875rem, 2vw, 1rem);
    color: var(--color-gold);
    text-transform: uppercase;
    letter-spacing: 3px;
    margin-bottom: 1rem;
  }

  .hero-title {
    font-size: clamp(2.25rem, 6vw, 3.75rem);
    color: var(--color-white);
    margin-bottom: 1rem;
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: var(--color-cream);
    opacity: 0.9;
    line-height: 1.6;
    margin-bottom: 2.5rem;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 90vh;
    }

    .hero-title {
      font-size: clamp(1.75rem, 8vw, 2.5rem);
    }

    .hero-actions .btn {
      width: 100%;
    }
  }
</style>
