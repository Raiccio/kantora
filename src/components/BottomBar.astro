<div class="bottombar">
  <div class="bottombar-inner container">
    <span class="bottombar-copy">
      &copy; {new Date().getFullYear()} Адвокатско съдружие "Георгиев и Зънгарова". Всички права запазени.
    </span>
    <span class="bottombar-phone">
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
      <a href="tel:+359894392323">0894 392 323</a>
    </span>
    <nav class="bottombar-links">
      <a href="/">Начало</a>
      <a href="/#about">За нас</a>
      <a href="/#practice">Практики</a>
      <a href="/#blog">Блог</a>
      <a href="/#contact">Контакти</a>
    </nav>
  </div>
</div>

<style is:global>
  .bottombar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    height: var(--bottombar-height);
    background: var(--color-navy-dark);
    color: var(--color-cream);
    font-size: 0.8125rem;
    z-index: 998;
    display: flex;
    align-items: center;
    border-top: 1px solid rgba(201, 168, 76, 0.15);
  }

  .bottombar-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    width: 100%;
    padding: 0 1.5rem;
  }

  .bottombar-copy {
    opacity: 0.6;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .bottombar-phone {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    white-space: nowrap;
  }

  .bottombar-phone svg {
    opacity: 0.7;
    flex-shrink: 0;
  }

  .bottombar-phone a {
    color: var(--color-gold);
    text-decoration: none;
    font-weight: 600;
  }

  .bottombar-phone a:hover {
    color: var(--color-gold-light);
  }

  .bottombar-links {
    display: flex;
    gap: 1.25rem;
  }

  .bottombar-links a {
    color: var(--color-cream);
    text-decoration: none;
    opacity: 0.7;
    transition: opacity 0.2s;
  }

  .bottombar-links a:hover {
    opacity: 1;
    color: var(--color-gold);
  }

  @media (max-width: 768px) {
    .bottombar-copy {
      display: none;
    }

    .bottombar-links {
      gap: 0.875rem;
    }

    .bottombar-links a {
      font-size: 0.75rem;
    }
  }
</style>
